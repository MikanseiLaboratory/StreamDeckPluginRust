# Counter Sample (Rust)

A keypad action and a Stream Deck + dial share one counter. The React inspector binds `label` / `increment` and sends `add` / `reset` / `refresh`.

```powershell
./publish.ps1 -Install -Pack
```

UUID: `dev.flowingspdg.countersample.rust`
